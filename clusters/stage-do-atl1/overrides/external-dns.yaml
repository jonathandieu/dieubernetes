# Unique owner-id per cluster. external-dns runs on every cluster against the
# shared dieu.dev zone with policy=sync; a shared txtOwnerId makes clusters
# without a given HTTPRoute delete records another cluster created (see #34).
external-dns:
  txtOwnerId: dieubernetes-stage
