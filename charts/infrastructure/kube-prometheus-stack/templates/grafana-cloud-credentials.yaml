apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-cloud-credentials
  namespace: {{ .Release.Namespace }}
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: onepassword
    kind: ClusterSecretStore
  target:
    name: grafana-cloud-credentials
  data:
    - secretKey: username
      remoteRef:
        key: wxho2nawl52ckxjlfiqqefgqwe/username
    - secretKey: password
      remoteRef:
        key: wxho2nawl52ckxjlfiqqefgqwe/password
